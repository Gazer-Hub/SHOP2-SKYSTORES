# secret used to verify webhook HMAC (recommended)
WEBHOOK_SECRET=change_this_to_a_random_string

# header name Moniepoint uses to send signature, e.g. x-signature or x-moniepoint-signature
SIGNATURE_HEADER=x-signature

# sqlite file (optional)
DB_FILE=./payments.db

# port
PORT=3000
